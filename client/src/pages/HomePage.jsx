import React from 'react';
import Search from '../components/Search';

const HomePage = () => {
    return (
        <>
            <Search onSubmit={() => { }} />
            <main>
                ...
            </main>
        </>
    );
};

export default HomePage;